#include<iostream>  
#include<string>  
#include<math.h>  
using namespace std;  
  
int main()  
{  
    string data,b;  
    int i=0,count,sum; 
    int j=0;
    getline(cin,data); 
    
    if (StringUtils.isEmpty(data))
        {
            return "";
        }
        
        int iTotal = 0;
        int iLen = data.length();
        int iNum = 0;
        
        
        return 0;
    
}  
